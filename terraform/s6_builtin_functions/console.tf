> "hello this is a string"
hello this is a string
> replace("hello this is a string","e","!")
h!llo this is a string
> "the server launched at timestamp()"
the server launched at timestamp()
> "the server launched at ${timestamp()}"
> list("a","b","c")
[
  "a",
  "b",
  "c",
]
